export type Sport = {
  id: string;
  name: string;
  slug: string;
  status: string;
  createdAt: string;
  updatedAt: string;
};

export type SportChipItem = {
  id: string;
  label: string;
  emoji: string;
};
